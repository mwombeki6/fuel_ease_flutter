// Copy this file to secrets.dart and fill in real values.
// secrets.dart is gitignored — never commit it.
// In CI: flutter build apk --dart-define=MAPBOX_TOKEN=pk.xxx

const kMapboxToken = 'pk.YOUR_MAPBOX_TOKEN_HERE';
